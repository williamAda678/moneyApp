# MoneyTrack Backend

C# ASP.NET Core backend for MoneyTrack – Personal Finance Manager.

## Features

- User registration/login with JWT authentication
- Transactions CRUD with user filtering
- SQLite database
- Swagger documentation

## Setup

1. Clone repo
2. Add `appsettings.json` with JWT config
3. Run `dotnet run`
